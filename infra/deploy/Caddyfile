{
  email {$ACME_EMAIL}
  admin off
}

{$APP_DOMAIN} {
  encode zstd gzip

  header {
    -Server
  }

  reverse_proxy web:3000 {
    health_uri /login
    health_interval 30s
    health_timeout 5s
  }

  log {
    output stdout
    format json
  }
}
